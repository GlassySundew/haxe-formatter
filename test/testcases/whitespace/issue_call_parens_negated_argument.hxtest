{
  "whitespace": {
    "parenConfig": {
      "callParens": {
        "openingPolicy": "onlyAfter",
        "closingPolicy": "before"
      }
    }
  }
}

---

foo( -bar );
foo( -42 );

---

foo( -bar );
foo( -42 );
